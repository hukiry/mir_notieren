
using UnityEngine;

public class TestExample : MonoBehaviour
{

    public void StartTest()
    {

        //GameViewResolution view = new GameViewResolution(UnityEditor.GameViewSizeGroupType.iOS);
        ////view.SetGameViewFixedResolution(1280, 720, "通用");
        ////var dic=view.GetCustomResolution();
        ////foreach (var item in dic)
        ////{
        ////    LogManager.Log(item.ToString());
        ////}
        //view.Remove(36);
        //LogManager.Log(view.Count());
    }
}


