<?xml version="1.0" encoding="utf-8"?>
<manifest 
	xmlns:android="http://schemas.android.com/apk/res/android"  
	package="com.merge.paradise" 
	xmlns:tools="http://schemas.android.com/tools"
	android:installLocation="preferExternal">
	<supports-screens 
		android:smallScreens="true" 
		android:normalScreens="true" 
		android:largeScreens="true" 
		android:xlargeScreens="true" 
		android:anyDensity="true" />

	<!-- 权限添加 -->
	<!-- 4G权限-->
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- 访问wifi-->
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
	<!-- 网络访问-->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<!-- 多个dex优化：androidx.multidex.MultiDexApplication -->
	<!-- 如果有启动壁纸，需要集成 androidx.multidex.MultiDexApplication-->
	<application
		android:label="@string/app_name"
        android:icon="@mipmap/app_icon"
		android:name = "androidx.multidex.MultiDexApplication"
		android:appComponentFactory="androidx.core.app.CoreComponentFactory"
		android:networkSecurityConfig="@xml/network_security_config">
		<!-- 自定义的启动类 com.yiliu.gamelibrary.MainActivity 需要集成 UnityPlayerActivity-->
		<activity
			android:name="com.yiliu.gamelibrary.MainActivity"
			android:theme="@style/UnityThemeSelector"
			android:screenOrientation="reverseLandscape"
			android:launchMode="singleTask"
			android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density"
			android:resizeableActivity="false"
			android:hardwareAccelerated="false"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
			<!--Unity元数据 支持场景，必须有 -->
			<meta-data android:name="unityplayer.UnityActivity" android:value="true" />
			<!--支持刘海屏，必须有 -->
			<meta-data android:name="android.notch_support" android:value="true" />
		</activity>
		<!--Unity模块节点移除：tools:node="remove"，打包时会自定义移除相同的启动activity -->
		<activity tools:node="remove" android:name="com.unity3d.player.UnityPlayerActivity" android:exported="true"/>
		<!--活动添加，继成Activity或后台服务-->
	</application>
</manifest>